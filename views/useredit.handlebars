{{!-- ***************************************************** USER EDITING A SINGLE POST VIEW *************************************************** --}}
<section class="hero has-background-grey-lighter">
    <div class="hero-body">
        <div class="container">
            <div class="columns is-centered">
                <div class="column">
                    <form action="" class="box">
                        <label style="text-align: center; font-size: 200%; border-bottom: 1px solid black" class="label is-large" for="">
                        Edit Post 📝
                        </label>
                            <div class="field">
                                <label style="font-size: 125%" for="" class="label">
                                    Subject / Title 🗃️
                                </label>
                                    <div class="control">
                                        <input type="text" placeholder="www.ssh.com...Blog Git before you even start...🎤" class="input subject-input"
                                        value="{{postData.title}}" required/>
                                    </div>
                                </div>
            <div class="field-body">
                <div class="field">
                    <label style="font-size: 150%; text-align: center; padding-top: 2.5%; border-bottom: solid 1px black" for="" class="label">Content 🗄️</label>
                    <div class="control" style="padding-top: 2.5%">
                    <div style="margin-bottom: 5%" class="card-img-top">
                        <img width="100%" src="{{postData.image}}" 
                            placeholder="Upload Image...📸"/>
                </div>
                <div id="inline_container"></div>
                    <div class="field-body">
                        <div class="field">
                            <textarea class="textarea content-input"
                                placeholder="Blog Git before you even start...🖥️" required/>
                                {{postData.content}}
                            </textarea>
                        </div>
                    </div>
                </div>
            </div>
        </div>
            <div class="field is-grouped is-grouped-right mt-3">
                <button id="edit-submit" class="button is-success edit-submit">
                    Submit 📬
                </button>
            </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
<div class="current-post-id is-invisible">{{postData.id}}</div>
</section>
<script src="/js/user-edit.js"></script>