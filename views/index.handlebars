{{!------------------------------------ HOMEPAGE IMAGE ---------------------------------------}}
        <div class="columns is-centered">
          <div class="column is-8 is-offset-3">
            <div class="card-img-top">
              <img id="homelogo" src="https://res.cloudinary.com/dhqsixgmo/image/upload/v1665528445/blog-it-images/home_nddhnq.png"/>
            </div>
          </div>
        </div>
{{!------------------------------------- NEWEST POST SPACE --------------------------------------------}}
<section class="hero">
  <div class="hero-body">
    <div class="container">
      <section class="section">
        <div class="columns">
          <div class="column is-8 is-offset-2" style="padding-top: 5%">
            <div class="card" style="width: 100%; padding-top: 3%; text-align: center">
            <h1 id="title" class="title"><a href="/post/{{posts.0.0.id}}">{{posts.0.0.title}}</a></h1>
            <img class="card-img-top" src="{{posts.0.0.image}}" alt="Card image">
            <div class="card-body">
              <h2>{{format_date posts.0.0.createdAt}}</h2>
              <h6>By: {{posts.0.0.user.username}}</h6>
              <p class="card-text">{{posts.0.0.content}}</p>
            </div>
          </div>
        </div>
      </div>
    </section>
{{!------------------------------------ *END* POST SPACE *END* ----------------------------------}}
<div class="is-divider"></div>
      </div>
    </div>
</section>
  {{!--------------------------------------------- POST MAP ----------------------------------------------}}
<section class="hero ">
  <div class="hero-body-neg">
    <div class="container">
      {{#each posts as |post|}}
        {{#if @first}}
           
      {{else}}
        {{> post-summary}}
          {{/if}}
      {{/each}}
    </div>
  </div>
</section>
<script src='/js/main.js'></script> 